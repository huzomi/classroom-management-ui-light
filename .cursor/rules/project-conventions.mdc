---
description: 项目架构与开发规范，生成/集成页面时自动遵守
alwaysApply: true
---

# 智慧运维教室管理系统 - 项目规范

## 技术栈
- Next.js 16 App Router + React 19 + TypeScript
- Tailwind CSS v4 + shadcn 风格组件库
- 图标：lucide-react
- 表单：react-hook-form + zod

## 布局
根布局已有 PlatformSidebar + PlatformHeader + main，页面只生成 main 内部的内容。

## 路径别名
所有 import 用 `@/` 开头（指向项目根）。

## UI 组件
优先使用 `@/components/ui` 已有组件（Button, Card, Dialog, Input, Select, Table, Tabs, Badge, Form, Label, Checkbox, Switch, Sheet, Popover, Tooltip, ScrollArea, Skeleton 等）。合并 className 用 `import { cn } from "@/lib/utils"`。

## 样式令牌（禁止硬编码颜色）
- 背景：bg-background, bg-card, bg-muted
- 文字：text-foreground, text-muted-foreground
- 边框：border-border
- 主色：bg-primary, text-primary-foreground
- 次要：bg-secondary, bg-accent

## 文件组织
- 页面：`app/<路由>/page.tsx`
- 业务组件：`components/<模块名>/xxx.tsx`
- 通用组件：`components/ui/xxx.tsx`
- 类型定义：`types/xxx.ts`
- 文件命名：kebab-case

## 新页面集成
创建页面后，在 `components/layout/platform-sidebar.tsx` 的 navItems 中注册菜单入口。

## v0 代码集成
当用户粘贴 v0 生成的代码时：
1. 将组件引用替换为项目已有的 @/components/ui 组件
2. 将硬编码颜色（bg-white, text-gray-500 等）替换为设计令牌
3. 确保使用 cn() 合并 className
4. 按文件组织规范放置文件
5. 在侧栏注册菜单入口
